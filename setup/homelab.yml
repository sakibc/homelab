all:
  children:
    main_server:
      hosts:
        sakibs-ubuntu-server.local:
          ansible_user: sakib
    ubuntu_vm:
      hosts:
        sakibs-ubuntu-vm.local:
          ansible_user: sakib
    pihole:
      hosts:
        pihole.local:
          ansible_user: sakib
